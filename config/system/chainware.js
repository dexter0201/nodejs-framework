(function () {
    'use strict';

    module.exports = function (dexter) {
        dexter.modules = [];
        dexter.middleware = {
            before: [],
            after: []
        };

        // Registering a module with basic middleware hoks for submodules

    };
}());